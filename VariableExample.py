import os
os.system('clear')

# A variable is a bucket, a container. It holds things. Generally it holds one thing at the time

#create a variable called full_name

full_name = "Merveille Koina"

print(full_name)